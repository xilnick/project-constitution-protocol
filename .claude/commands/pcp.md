---
description: Prevent token bloat and merge conflicts in AI-driven workspaces. Activate the pcp skill and load the full Project Constitution Protocol.
argument-hint: [optional task or shortcode]
---

Invoke the `pcp:pcp` skill via the Skill tool, passing `$ARGUMENTS` as its args, then follow its
instructions. This file is an alias only: the skill is the protocol's single source, so never restate
any of it here.
