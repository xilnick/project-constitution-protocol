{
  "name": "toolbelt",
  "description": "Three habits that decide what an agent costs, for Antigravity CLI: parallel (fan independent work out into one wave), tokensave (ask the code graph instead of reading files), and search-tools (route each search to the tool that answers it cheapest)."
}
