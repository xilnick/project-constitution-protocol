## Anti-thrash

If the same failure survives two distinct fixes, stop varying details. Report the verbatim error
and what you think it means; that is the `hidden-coupling` trigger, and escalation is the
orchestrator's call, not a third attempt.
